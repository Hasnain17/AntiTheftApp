<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Default" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/gray_100</item>
        <item name="colorPrimaryDark">@color/white</item>
        <item name="colorAccent">@color/gray_new</item>
        <item name="android:statusBarColor">@color/newOrange_3</item>
        <!-- Customize your theme here. -->
    </style>

</resources>